@model PseudoCell.Models.GameDetailsView

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>GameDetails</h2>

<div>
    <h4>Game</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Game.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Game.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Game.CreatedDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Game.CreatedDate)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Game.CreatedBy)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Game.CreatedBy)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Back", "Index", null, new { @class = "btn btn-default" })
    @if(Model.IsStudent==false){
        @Html.ActionLink("Edit", "Edit", new { gameId = Model.Game.Id }, new { @class = "btn btn-default" })
    }
</p>
